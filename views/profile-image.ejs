<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>NextScrimz | Profile Image Selection</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: linear-gradient(135deg, #0c0c0c 0%, #181823 100%);
      color: #fff;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      padding: 20px;
    }

    .container {
      width: 100%;
      max-width: 800px;
      text-align: center;
    }

    .logo {
      margin-bottom: 1rem;
      font-size: 2.5rem;
      color: #cdb44e;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 0.5rem;
      background: linear-gradient(45deg, #ffd56b, #cdb44e);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      text-shadow: 0 2px 10px #b09f54b0;
      /* text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2); */
    }

    .subtitle {
      color: #a0a0a0;
      margin-bottom: 3rem;
      font-size: 1.1rem;
    }

    .carousel-container {
      position: relative;
      width: 100%;
      height: 350px;
      perspective: 1000px;
      margin: 2rem 0;
      touch-action: pan-y;
    }

    .carousel {
      position: absolute;
      width: 100%;
      height: 100%;
      transform-style: preserve-3d;
      transition: transform 0.8s cubic-bezier(0.17, 0.67, 0.83, 0.67);
    }

    .profile {
      position: absolute;
      width: 200px;
      height: 200px;
      left: 50%;
      top: 50%;
      margin-left: -100px;
      margin-top: -100px;
      border-radius: 50%;
      overflow: hidden;
      cursor: pointer;
      transition: transform 0.5s ease, opacity 0.5s ease, box-shadow 0.5s ease;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
      border: 3px solid transparent;
    }

    .profile img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.5s ease;
    }

    .profile:hover img {
      transform: scale(1.05);
    }

    .profile-name {
      position: absolute;
      bottom: -40px;
      left: 0;
      right: 0;
      text-align: center;
      font-weight: 600;
      opacity: 0;
      transition: opacity 0.3s ease;
      color: #fff;
      text-shadow: 0 0 8px rgba(0, 0, 0, 0.8);
    }

    .profile.active .profile-name {
      opacity: 1;
    }

    .profile.active {
      transform: translateZ(0px) scale(1);
      z-index: 10;
      box-shadow: 0 0 30px #b1a15d;
      border: 3px solid #f4da74;
    }

    .profile.prev {
      transform: translateX(-250px) translateZ(-200px) scale(0.7);
      opacity: 0.7;
      z-index: 2;
      box-shadow: 0 0 15px #cdb44e5a;
    }

    .profile.next {
      transform: translateX(250px) translateZ(-200px) scale(0.7);
      opacity: 0.7;
      z-index: 2;
      box-shadow: 0 0 15px #cdb44e5a;
    }

    .nav-btn {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      background: rgba(255, 255, 255, 0.1);
      border: none;
      color: white;
      font-size: 2rem;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      cursor: pointer;
      z-index: 100;
      transition: all 0.3s ease;
      backdrop-filter: blur(5px);
    }

    .nav-btn:hover {
      background: rgba(255, 255, 255, 0.2);
      transform: translateY(-50%) scale(1.1);
    }

    .prev-btn {
      left: 10%;
    }

    .next-btn {
      right: 10%;
    }

    .select-btn {
      margin-top: 2rem;
      padding: 12px 30px;
      background: transparent;
      border: 2px solid #e6c11e;
      border-radius: 30px;
      color: #e6c11e;
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
    }

    .select-btn:hover {
      transform: translateY(-3px);
      color: white;
      background-color: #baa237;
      box-shadow: 0 2px 20px #e6c11e56;
    }

    .select-btn:active {
      transform: translateY(0);
    }

    .indicator {
      display: flex;
      justify-content: center;
      margin-top: 20px;
      gap: 10px;
    }

    .dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.3);
      transition: all 0.3s ease;
    }

    .dot.active {
      background: #4ecdc4;
      transform: scale(1.3);
    }

    .footer {
      margin-top: 3rem;
      color: #666;
      font-size: 0.9rem;
    }

    .swipe-hint {
      display: none;
      margin-top: 20px;
      color: #4ecdc4;
      font-size: 0.9rem;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0% {
        opacity: 0.6;
      }

      50% {
        opacity: 1;
      }

      100% {
        opacity: 0.6;
      }
    }

    /* Mobile and Tablet Styles */
    @media (max-width: 768px) {
      .profile {
        width: 150px;
        height: 150px;
        margin-left: -75px;
        margin-top: -75px;
      }

      .profile.prev {
        transform: translateX(-180px) translateZ(-200px) scale(0.7);
      }

      .profile.next {
        transform: translateX(180px) translateZ(-200px) scale(0.7);
      }

      .nav-btn {
        display: none;
      }

      h1 {
        font-size: 2rem;
      }

      .swipe-hint {
        display: block;
      }
    }

    @media (max-width: 480px) {
      .profile {
        width: 130px;
        height: 130px;
        margin-left: -60px;
        margin-top: -60px;
      }

      .profile.prev {
        transform: translateX(-140px) translateZ(-200px) scale(0.6);
      }

      .profile.next {
        transform: translateX(140px) translateZ(-200px) scale(0.6);
      }

      h1 {
        font-size: 1.8rem;
      }

      .subtitle {
        font-size: 1rem;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="logo">
      <i class="fas fa-user-circle"></i>
    </div>
    <h1>Choose Your Profile</h1>
    <p class="subtitle">Select your avatar to continue to your account</p>

    <div class="carousel-container" id="carousel-container">
      <div class="carousel">
        <div class="profile prev">
          <img src="./profile/profile-1.jpg" alt="Profile-1">
          <div class="profile-name">Sophia</div>
        </div>
        <div class="profile active">
          <img src="./profile/profile-2.jpg" alt="Profile-2">
          <div class="profile-name">Emma</div>
        </div>
        <div class="profile next">
          <img src="./profile/profile-3.jpg" alt="Profile-3">
          <div class="profile-name">Olivia</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-4.jpg" alt="Profile-4">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-5.jpg" alt="Profile-5">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-6.jpg" alt="Profile-6">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-7.jpg" alt="Profile-7">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-8.jpg" alt="Profile-8">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-9.jpg" alt="Profile-9">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-10.jpg" alt="Profile-10">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-11.jpg" alt="Profile-11">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-12.jpg" alt="Profile-12">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-13.jpg" alt="Profile-13">
          <div class="profile-name">Ava</div>
        </div>
        <div class="profile">
          <img src="./profile/profile-14.jpg" alt="Profile-14">
          <div class="profile-name">Ava</div>
        </div>

      </div>

      <button class="nav-btn prev-btn" style="display: none;">&lt;</button>
      <button class="nav-btn next-btn"  style="display: none;">&gt;</button>
    </div>

    <div class="swipe-hint">
      <i class="fas fa-arrows-alt-h"></i> Swipe left or right to navigate
    </div>

    <div class="indicator">
      <div class="dot active"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
    </div>

    <button class="select-btn">Select Profile</button>

    <div class="footer">
      <p>© 2023 Profile Selection Login | Privacy Policy</p>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const carousel = document.querySelector('.carousel');
      const profiles = document.querySelectorAll('.profile');
      const dots = document.querySelectorAll('.dot');
      
      const prevBtn = document.querySelector('.prev-btn');
      const nextBtn = document.querySelector('.next-btn');

      const selectBtn = document.querySelector('.select-btn');
      const carouselContainer = document.getElementById('carousel-container');

      let currentIndex = 1;
      let startX = 0;
      let endX = 0;

      function updateCarousel() {
        profiles.forEach(profile => {
          profile.className = 'profile';
        });

        dots.forEach((dot, index) => {
          dot.classList.remove('active');
          if (index === currentIndex) {
            dot.classList.add('active');
          }
        });

        profiles[currentIndex].classList.add('active');

        if (currentIndex > 0) {
          profiles[currentIndex - 1].classList.add('prev');
        } else {
          profiles[profiles.length - 1].classList.add('prev');
        }

        if (currentIndex < profiles.length - 1) {
          profiles[currentIndex + 1].classList.add('next');
        } else {
          profiles[0].classList.add('next');
        }

        profiles.forEach((profile, index) => {
          if (index !== currentIndex &&
            index !== (currentIndex > 0 ? currentIndex - 1 : profiles.length - 1) &&
            index !== (currentIndex < profiles.length - 1 ? currentIndex + 1 : 0)) {
            profile.style.opacity = '0';
            profile.style.zIndex = '1';
          } else {
            profile.style.opacity = '';
            profile.style.zIndex = '';
          }
        });
      }

      nextBtn.addEventListener('click', () => {
        currentIndex = (currentIndex + 1) % profiles.length;
        updateCarousel();
      });

      prevBtn.addEventListener('click', () => {
        currentIndex = (currentIndex - 1 + profiles.length) % profiles.length;
        updateCarousel();
      });

      dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
          currentIndex = index;
          updateCarousel();
        });
      });

      selectBtn.addEventListener('click', () => {
        const activeProfile = document.querySelector('.profile.active img').alt;
        // alert(`Selected profile: ${activeProfile.toLowerCase()}`);
        selectedProfileImage(activeProfile.toLowerCase())
      });

      async function selectedProfileImage(image) {
        try {
          const res = await fetch("/set-image", {
            method: "POST",
            headers: {
              "Content-Type": "application/json"
            },
            body: JSON.stringify({image})
          });
          const data = await res.json();
  
          if (res.ok) {
            window.location.href = data.redirectedTo
          } else {
            console.log(data.message)
          }

        } catch (err) {
          console.log("Some error occured!")
        }
      }

      document.addEventListener('keydown', (e) => {
        if (e.key === 'ArrowRight') {
          nextBtn.click();
        } else if (e.key === 'ArrowLeft') {
          prevBtn.click();
        } else if (e.key === 'Enter') {
          selectBtn.click();
        }
      });

      carouselContainer.addEventListener('touchstart', (e) => {
        startX = e.touches[0].clientX;
      }, {
        passive: true
      });

      carouselContainer.addEventListener('touchmove', (e) => {
        endX = e.touches[0].clientX;
      }, {
        passive: true
      });

      carouselContainer.addEventListener('touchend', () => {
        if (startX - endX > 50) {
          nextBtn.click();
        } else if (endX - startX > 50) {
          prevBtn.click();
        }
      });

      carouselContainer.addEventListener('mousedown', (e) => {
        startX = e.clientX;
        carouselContainer.style.cursor = 'grabbing';
      });

      carouselContainer.addEventListener('mousemove', (e) => {
        if (e.buttons === 1) {
          endX = e.clientX;
        }
      });

      carouselContainer.addEventListener('mouseup', () => {
        if (startX - endX > 50) {
          nextBtn.click();
        } else if (endX - startX > 50) {
          prevBtn.click();
        }
        carouselContainer.style.cursor = 'grab';
      });

      carouselContainer.addEventListener('mouseleave', () => {
        carouselContainer.style.cursor = 'grab';
      });

      updateCarousel();

      
    });
  </script>
</body>

</html>