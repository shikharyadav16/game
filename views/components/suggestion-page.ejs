<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .suggestions-heading {
            color: white;
            display: flex;
            font-family: "Nunito", sans-serif;
            font-weight: 700;
            font-size: 16px;
            margin-top:40px;
            align-items: center;
            height: 45px;
            margin-left: 80px;
        }
        .suggestions-heading i {
            cursor: pointer;
            margin-top: 2px;
        }
        @media (max-width: 700px) {
            .suggestions-heading {
                margin-left: 30px;
            }
        }

        .suggestions-container {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 350px;
            max-width: 100vw;
            width: 100%;
            flex-direction: column;
            position: relative;
        }

        .suggestions-body {
            border-radius: 12px;
            font-family: "Nunito", sans-serif;
            display: flex;
            gap: 15px;
            flex-direction: column;
            height: 100%;
            background-color: white;
            max-width: 500px;
            align-items: center;
            justify-content: center;
            border: 2px solid rgba(0, 0, 0, 0.582);
            width: 90%;
        }

        .suggestions-body>div {
            width: 90%;
        }

        .suggestions-body>div>div {
            color: rgba(0, 0, 0, 0.852);
            font-size: 12px;
            font-weight: bold;
            margin-left: 2px;
            text-transform: uppercase;
        }

        .suggestions-body input {
            color: rgba(0, 0, 0, 0.812);
            margin-top: 5px;
            border: 1px solid rgba(0, 0, 0, 0.1);
            background-color: #f9f9f9;
            width: calc(100% - 20px);
            border-radius: 15px;
            padding-left: 20px;
            height: 45px;
            font-weight: 700;
            font-size: 18px;
            font-family: "Nunito", sans-serif;
        }

        .suggestions-body textarea {
            width: 100%;
            color: rgba(0, 0, 0, 0.812);
            margin-top: 5px;
            border: 1px solid rgba(0, 0, 0, 0.1);
            background-color: #f9f9f9;
            width: calc(100% - 30px);
            border-radius: 15px;
            height: 45px;
            font-weight: 700;
            resize: none;
            font-size: 18px;
            font-family: "Nunito", sans-serif;
            height: 100px;
            padding: 15px;
        }

        #suggestion-btn {
            font-size: 16px;
            text-transform: uppercase;
            font-weight: bold;
            background-color: #bc892b;
            height: 50px;
            width: 90%;
            font-family: "Poppins", sans-serif;
            text-align: center;
            align-content: center;
            color: white;
            border-radius: 15px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
            cursor: pointer;
        }

        #suggestion-btn:hover {
            background-color: #b07f24;
        }
    </style>
</head>

<body>

    <div class="suggestions-heading">Suggestions&nbsp;<i class="ph ph-info"></i></div>
    <div class="suggestions-container">
        <div class="suggestions-body">
            <div>
                <div>Name:</div>
                <input type="text" id="suggestion-name">
            </div>
            <div>
                <div>Suggestion:</div>
                <textarea id="suggestion-text-area"></textarea>
            </div>
            <div id="suggestion-btn">Send</div>
        </div>
    </div>
</body>

</html>